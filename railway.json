{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Railway 部署配置 - 从 Render 迁移",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && pip install -r requirements.txt && bash install_chinese_fonts.sh || true && bash install_playwright.sh"
  },
  "deploy": {
    "startCommand": "cd server && uvicorn app:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}

