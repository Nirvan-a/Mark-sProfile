# Cloudflare Pages 配置说明

## 📋 配置字段说明

Cloudflare Pages 的配置界面可能因版本不同而略有差异，以下是常见的字段名称：

### 方式 1：在初始设置页面

在 "Set up builds" 页面，可能看到以下字段之一：

- **"Build output directory"** 
- **"Output directory"**
- **"Publish directory"**
- **"Directory"**

**填写值**：`web/dist`

### 方式 2：如果看不到这个字段

可能需要在 "Settings" 中配置：

1. 先完成初始部署（使用默认配置）
2. 部署完成后，进入项目页面
3. 点击 **"Settings"** 标签页
4. 找到 **"Builds & deployments"** 部分
5. 点击 **"Edit configuration"** 或 **"Configure build"**
6. 在这里可以修改：
   - Build command
   - Output directory

---

## 🔧 完整配置步骤

### 步骤 1：初始配置

在 "Set up builds" 页面：

1. **Framework preset**: 选择 **"None"** 或 **"Vite"**（如果有）
2. **Build command**: 
   ```
   cd web && npm install && npm run build
   ```
3. **Output directory**: 
   ```
   web/dist
   ```
   （如果看不到这个字段，先继续下一步）

### 步骤 2：如果看不到 Output directory 字段

**选项 A**：先部署，然后修改
1. 先点击 "Save and Deploy"（使用默认配置）
2. 部署完成后，在项目设置中修改

**选项 B**：检查是否有其他字段
- 查看是否有 "Root directory" 字段
- 查看是否有 "Build settings" 展开选项
- 查看页面底部是否有 "Show advanced options"

### 步骤 3：部署后修改配置

如果初始部署时没有配置 Output directory：

1. 进入你的 Pages 项目
2. 点击 **"Settings"** 标签页
3. 找到 **"Builds & deployments"** 部分
4. 点击 **"Configure build"** 或 **"Edit"**
5. 修改：
   - **Build command**: `cd web && npm install && npm run build`
   - **Output directory**: `web/dist`
6. 保存并重新部署

---

## 🎯 快速解决方案

### 如果找不到 Output directory 字段：

**方案 1**：先部署，后修改
1. Build command 填写：`cd web && npm install && npm run build`
2. 其他字段保持默认
3. 点击 "Save and Deploy"
4. 部署完成后，在 Settings 中修改 Output directory

**方案 2**：检查页面
- 向下滚动，查看是否有更多选项
- 查看是否有 "Advanced" 或 "Show more" 按钮
- 查看是否有 "Environment variables" 下方的配置

**方案 3**：使用默认配置
- 如果 Cloudflare 自动检测到是 Vite 项目，可能会自动配置
- 先部署看看是否能正常工作

---

## 📝 当前应该填写的配置

无论界面如何，确保以下配置正确：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **Build command** | `cd web && npm install && npm run build` | 必须填写 |
| **Output directory** | `web/dist` | 如果找不到，部署后修改 |
| **Root directory** | 留空 | 不填写 |

---

## ✅ 检查清单

- [ ] Build command 已填写：`cd web && npm install && npm run build`
- [ ] 如果看到 Output directory 字段，填写：`web/dist`
- [ ] 如果看不到，先部署，然后在 Settings 中修改
- [ ] 点击 "Save and Deploy"

---

## 🆘 如果还是找不到

告诉我：
1. 你看到的页面有哪些字段？
2. 是否有 "Settings" 或 "Configure" 按钮？
3. 或者直接先部署，我们部署后再修改配置

我可以根据你的具体情况提供更准确的指导。

