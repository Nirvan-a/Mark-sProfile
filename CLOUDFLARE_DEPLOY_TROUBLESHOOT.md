# Cloudflare Pages 部署失败排查指南

## 🔍 检查清单

### 1. 查看具体错误信息
在 Cloudflare Dashboard：
1. 进入你的 Pages 项目
2. 点击失败的部署
3. 查看 "Build logs" 或 "Deployment logs"
4. 找到具体的错误信息

### 2. 常见错误及解决方案

#### 错误 A：找不到构建产物
**错误信息**：`No output directory found` 或类似

**解决方案**：
1. 进入 Settings → Builds & deployments
2. 确认 "Output directory" 设置为：`web/dist`
3. 如果看不到这个字段，在 Build command 中确保：
   ```bash
   cd web && npm install && npm run build
   ```

#### 错误 B：文件太大
**错误信息**：`File too large` 或 `Size limit exceeded`

**解决方案**：
- Cloudflare Pages 单个文件限制 25MB
- 检查 `web/dist` 目录是否有大文件
- 优化图片和资源

#### 错误 C：路由配置问题
**错误信息**：部署成功但页面 404

**解决方案**：
- 已添加 `_redirects` 文件（已推送）
- 确保文件在 `web/public/_redirects`
- Vite 会自动复制到 `dist` 目录

#### 错误 D：构建命令失败
**错误信息**：构建阶段失败

**解决方案**：
- 检查 Node.js 版本（需要 18+）
- 检查依赖是否正确安装
- 查看构建日志中的具体错误

---

## 🛠️ 立即操作步骤

### 步骤 1：检查构建配置
1. 进入 Cloudflare Dashboard
2. 进入 Pages 项目 → Settings
3. 找到 "Builds & deployments"
4. 确认配置：
   - **Build command**: `cd web && npm install && npm run build`
   - **Output directory**: `web/dist`
   - **Root directory**: 留空

### 步骤 2：查看错误日志
1. 进入 "Deployments" 标签页
2. 点击失败的部署
3. 查看详细错误信息
4. 告诉我具体的错误信息

### 步骤 3：重新部署
1. 如果修改了配置，点击 "Retry deployment"
2. 或者等待自动重新部署（已推送 `_redirects` 文件）

---

## 📝 需要的信息

请告诉我：
1. **具体的错误信息**（从部署日志中复制）
2. **失败发生在哪个阶段**：
   - 构建阶段（Building）
   - 部署阶段（Deploying）
3. **Build command 和 Output directory 的配置**

这样我可以提供更准确的解决方案。

---

## 💡 临时解决方案

如果问题持续，可以尝试：

### 方案 1：使用 Cloudflare Pages Functions
如果 SPA 路由有问题，可以使用 Functions 处理路由。

### 方案 2：检查文件结构
确保构建后的 `dist` 目录结构正确：
```
dist/
  ├── index.html
  ├── assets/
  └── _redirects
```

### 方案 3：简化构建
如果依赖有问题，可以尝试：
```bash
cd web && npm ci && npm run build
```

