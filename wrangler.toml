# Cloudflare Worker 配置文件
name = "api-proxy"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

# 路由配置 - 重要：确保请求路由到Worker
[[routes]]
pattern = "api.mazhaofeng.com/*"
zone_name = "mazhaofeng.com"

# Worker 配置
[build]
command = "echo 'Worker ready'"
